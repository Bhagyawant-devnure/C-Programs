/*write a c program to check and print the maximum number two numbers without using ternary operator and
control statement*/
#include<stdio.h>
int main()
{
    int a=20;
    int b=10;
    (a<b) && printf("b is big") || printf("a is big");
}