//Write a C program to find maximum between two number using only if


#include<stdio.h>
#include<conio.h>
int main(){
    
    int a;
    int b;
    printf("enter no a");
    scanf("%d",&a);
    printf("enter no b");
    scanf("%d",&b);
    if(a>=b  && printf("A is big") || printf("B is big"));    
}